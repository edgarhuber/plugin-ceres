{{ component( "Ceres::MyAccount.Components.OrderHistory" ) }}
{{ component( "Ceres::MyAccount.Components.OrderReturnHistory" ) }}

<script type="x/template" id="vue-history">
	<div class="page-content basket history container-max">
		<div class="row justify-content-center bg-primary-3">
			<div class="col-md-8 col-lg-6 d-flex">
				<div class="card card-body">
					<h4>{{ trans("Ceres::Template.orderHistory") }}</h4>
					<div>
						<order-history template="#vue-order-history">
						</order-history>
					</div>
				</div>
			</div>
		</div>
		<div class="row justify-content-center bg-primary-3">
			<div class="col-md-8 col-lg-6 d-flex">
				<div class="card card-body mb-0">
					<div id="order-return-tab" v-if="isReturnActive && contactHasReturns">
						<h4>{{ trans("Ceres::Template.returnHistory") }}</h4>
			        	<order-return-history
		                    template="#vue-order-return-history"
		                    :items-per-page="ordersPerPage"
		                    :show-first-page="{{ config('Ceres.pagination.showFirstPage') }}"
		                    :show-last-page="{{ config('Ceres.pagination.showLastPage') }}">
			            </order-return-history>
			        </div>
			        <div id="order-return-tab2" v-if="isReturnActive && !contactHasReturns">
						<h4>{{ trans("Ceres::Template.returnHistory") }}</h4>
			        	<div class="card rounded-sm mb-0 p-1-rem text-small">
					        <div>Noch keine Retouren vorhanden</div>
				        </div>
			        </div>
				</div>
			</div>
		</div>
	</div>
</script>